<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/mapFragment">

    <fragment
        android:id="@+id/mapFragment"
        android:name="com.vima.studyspots.MapFragment"
        android:label="fragment_map"
        tools:layout="@layout/fragment_map" >
        <action
            android:id="@+id/action_mapFragment_to_studyListFragment"
            app:destination="@id/studyListFragment" />
    </fragment>
    <fragment
        android:id="@+id/studyListFragment"
        android:name="com.vima.studyspots.StudyListFragment"
        android:label="fragment_study_list"
        tools:layout="@layout/fragment_study_list" />
</navigation>